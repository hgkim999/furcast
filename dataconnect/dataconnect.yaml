specVersion: "v1beta"
serviceId: "furcast"
location: "us-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "furcast-server-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
